# Skill definition — reference for automated setup.
# For the live demo (Act 3), just upload SKILL.md in the portal UI — no tools config needed.
name: order-api-runbook
description: >
  Use when investigating issues with the order-api service including
  500 errors, latency spikes, security findings, and secret leaks.
  Contains known issue patterns, remediation steps, and escalation policy.
files:
  - SKILL.md
tools:
  - RunAzCliReadCommands
  - QueryAppInsightsByResourceId
  - QuerySourceBySemanticSearch
  - CreateGithubIssue
